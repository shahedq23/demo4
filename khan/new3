import pygame
import math

pygame.init()
WIDTH, HEIGHT = 800, 600
screen = pygame.display.set_mode((WIDTH, HEIGHT))
clock = pygame.time.Clock()

# Bus state
bus_pos = [100, 100]
bus_angle = 0
bus_speed = 2

# Route waypoints
waypoints = [(100, 100), (700, 100), (700, 500), (100, 500)]
wp_index = 0

def draw_bus(x, y, angle):
    bus = pygame.

