<!DOCTYPE html>
<html>
<head>
  <title>Smart TV AI</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #111;
      color: #fff;
      text-align: center;
      padding-top: 50px;
    }
    .tv-box {
      background-color: #222;
      border: 2px solid #444;
      padding: 30px;
      width: 400px;
      margin: auto;
      border-radius: 10px;
    }
    h1 {
      color: #0f0;
    }
    button {
      margin-top: 20px;
      padding: 10px 20px;
      font-size: 16px;
      background-color: #0f0;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    button:hover {
      background-color: #0c0;
    }
  </style>
</head>
<body>

<div class="tv-box">
  <h1>Smart TV AI</h1>
  <p id="current-time">Checking time...</p>
  <p id="recommendation">Analyzing...</p>
  <button onclick="recommendChannel()">Get Recommendation</button>
</div>

<script>
function recommendChannel() {
  const now = new Date();
  const hour = now.getHours();
  document.getElementById('current-time').innerText = "Current Time: " + hour + ":00";

  let channel;

  if (hour >= 6 && hour < 9) {
    channel = "📰 News Channel";
  } else if (hour >= 9 && hour < 17) {
    channel = "📚 Educational Channel";
  } else if (hour >= 17 && hour < 21) {
    channel = "🎭 Entertainment Channel";
  } else {
    channel = "🎬 Movie Channel";
  }

  document.getElementById("recommendation").innerText = "Recommended: " + channel;
}
</script>

</body>
</html>

